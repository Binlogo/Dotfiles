[tools]
node = "lts"
ruby = "3.4"
java = "temurin-17"
git-lfs = "latest"
# Upgrade via `mise use -g bun@latest` — `bun upgrade` bypasses mise.
bun = "latest"

[settings]

[settings.ruby]
compile = false
github_attestations = false
